<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextHint" parent="TextAppearance.AppCompat">
        <item name="colorAccent">@android:color/white</item>
        <item name="android:textColorHint">@color/WhiteColor</item>
        <item name="colorControlNormal">@color/WhiteColor</item>
        <item name="colorControlActivated">@color/WhiteColor</item>
        <item name="colorControlHighlight">@color/WhiteColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--ADD EVENT ACTIONBAR-->

    <style name="AddEventActionbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/Event.Widget.AppTheme.ActionBar</item>
        <item name="actionBarTheme">@style/Event.ThemeOverlay.AppTheme.ActionBar</item>
        <item name="actionBarPopupTheme">@style/Event.ThemeOverlay.AppTheme.PopupMenu</item>
    </style>
    <style name="Event.Widget.AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <!-- action bar background - without android prefix -->
        <item name="background">@color/btnbackground</item>
        <!-- action bar elevation - without android prefix -->
        <item name="elevation">4dp</item>
    </style>
    <style name="Event.ThemeOverlay.AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <item name="android:textColorPrimary">@color/WhiteColor</item>
        <!-- subtitle text color -->
        <item name="android:textColorSecondary">?android:textColorPrimary</item>
        <!-- action menu item text color -->
        <item name="actionMenuTextColor">?android:textColorPrimary</item>
        <!-- action menu item icon color -->
        <item name="colorControlNormal">?android:textColorPrimary</item>
    </style>
    <style name="Event.ThemeOverlay.AppTheme.PopupMenu" parent="ThemeOverlay.AppCompat.Dark">
        <!-- popup menu background - NEVER "android:background" !!! in themes -->
        <item name="android:colorBackground">@color/btnbackground</item>
        <!-- popup menu item text color -->
        <item name="android:textColorPrimary">@color/WhiteColor</item>
    </style>

    <!--end add event action bar-->

    <!--edit USER actionbar-->
    <style name="EditUserActionbarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/EditUser.Widget.AppTheme.ActionBar</item>
        <item name="actionBarTheme">@style/EditUser.ThemeOverlay.AppTheme.ActionBar</item>
        <item name="actionBarPopupTheme">@style/EditUser.ThemeOverlay.AppTheme.PopupMenu</item>
    </style>
    <style name="EditUser.Widget.AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <!-- action bar background - without android prefix -->
        <item name="background">@color/orange</item>
        <!-- action bar elevation - without android prefix -->
        <item name="elevation">4dp</item>
    </style>
    <style name="EditUser.ThemeOverlay.AppTheme.ActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <item name="android:textColorPrimary">@color/WhiteColor</item>
        <!-- subtitle text color -->
        <item name="android:textColorSecondary">?android:textColorPrimary</item>
        <!-- action menu item text color -->
        <item name="actionMenuTextColor">?android:textColorPrimary</item>
        <!-- action menu item icon color -->
        <item name="colorControlNormal">?android:textColorPrimary</item>
    </style>
    <style name="EditUser.ThemeOverlay.AppTheme.PopupMenu" parent="ThemeOverlay.AppCompat.Dark">
        <!-- popup menu background - NEVER "android:background" !!! in themes -->
        <item name="android:colorBackground">@color/orange</item>
        <!-- popup menu item text color -->
        <item name="android:textColorPrimary">@color/WhiteColor</item>
    </style>
    <!--end edit USERACTIONBAR-->
</resources>
